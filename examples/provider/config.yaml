apiVersion: v1
kind: Secret
metadata:
  name: akash-wallet-secret
  namespace: crossplane-system
type: Opaque
stringData:
  # Your Akash wallet private key (hex-encoded).
  # Export from an existing key:
  #   akash keys export <key-name> --keyring-backend os --unarmored-hex --unsafe
  privateKey: "<your-wallet-private-key-hex-here>"

---

apiVersion: akash.overlock.network/v1alpha1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: akash-wallet-secret
      key: privateKey
  configuration:
    keyName: "default"
    keyringBackend: "os"
    net: "mainnet"
    chainId: "akashnet-2"
    node: "https://rpc.akashnet.io:443"
    providersApi: "https://akash-api.polkachu.com"
