apiVersion: akash.overlock.network/v1alpha1
kind: Certificate
metadata:
  name: web-app-certificate
  namespace: default
spec:
  providerConfigRef:
    name: default
  forProvider:
    domains:
      - example.com
      - www.example.com
    autoRenew: true
    validityDays: 365